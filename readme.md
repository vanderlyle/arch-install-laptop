# Install instructions

```bash
loadkeys hu
pacman -Sy git --noconfirm
git clone https://github.com/vanderlyle/arch-install-laptop --depth=1
cd arch-install-laptop
./install.sh
```

## Issues
- Make sure hdd is **/dev/sda** by running `fdisk -l`
